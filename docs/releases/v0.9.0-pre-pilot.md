# Release v0.9.0-pre-pilot

Đây là bản snapshot (điểm lưu) trước khi hệ thống DESEMBRE Partner Hub bước vào giai đoạn chạy thử nghiệm thật (Pilot).

## Mục tiêu

Tạo ra một phiên bản ổn định, đã được test QA tĩnh (Modal, Drawer) và Fix 100% các Blocker nghiêm trọng (Lỗi double submit, lỗi thiếu Role Guard, kẹt form State). Không có tính năng mới.

## Build Status

- `npm run build`: **PASS**
- Typescript Check: **PASS** (Các cảnh báo cũ được giữ lại theo Legacy, không phát sinh Blocker mới).
- Git Tag: `v0.9.0-pre-pilot`
- Commit Hash: `be82b43c48b3da646ae3ac95f81f5df19102fdc9`

## Cấu trúc lưu trữ Backup

Tham khảo:

- `pilot_backup_checklist.md`: Danh sách DB Table cần sao lưu.
- `pilot_rollback_plan.md`: Quy trình xử lý khi gặp sự cố.
